<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RecogniseTablet.Views.AddFacePage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>


            <StackLayout Grid.Row="0">
                <Button Text="Register a face"
                    Command="{Binding TakePhotoRegisterCommand}"/>

                <Button Text="Detect Face"
                        Command="{Binding TakePhotoIdentifyCommand}"/>

            </StackLayout>


            <Image Grid.Row="1" x:Name="TakenImage"
                    Source="{Binding ImageLocation}"/>
            <Label Grid.Row="1" Text="{Binding FoundUser}" FontSize="Medium"/>
                


        </Grid>

    </ContentPage.Content>
</ContentPage>